# 复制为 .env 后填入自己的密钥。.env 不会进仓库。
# 在 https://www.opensubtitles.com 注册后，于 Profile -> API Consumers 生成。
# 没有 EXPO_PUBLIC_ 前缀，所以它只留在开发服务器进程里，不会被打进客户端 bundle。
# 字幕请求由 metro.config.js 挂载的本地代理转发（OpenSubtitles 会按 User-Agent 拦截浏览器）。
OPENSUBTITLES_API_KEY=

# 电影库根目录，填了以后字幕就存到视频文件旁边，顺带生成一份通用双语 srt，
# PotPlayer、VLC 之类的播放器也能直接加载。
# 留空则沿用项目里的 subtitles/ 目录。浏览器不会告诉网页视频在哪个文件夹，
# 所以要靠这个目录按文件名把片源找回来。
MOVIE_ROOT=
